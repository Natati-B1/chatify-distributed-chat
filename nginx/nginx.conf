worker_processes auto;

events {
  worker_connections 1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;

  map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
  }

  upstream chatify_nodes {
    # Round Robin (default) — sticky sessions not required when Socket.io uses Redis adapter
    server chat-server-1:3000;
    server chat-server-2:3000;
    server chat-server-3:3000;
  }

  server {
    listen 80;
    server_name _;

    location / {
      proxy_pass http://chatify_nodes;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_cache_bypass $http_upgrade;

      proxy_read_timeout 86400s;
      proxy_send_timeout 86400s;
    }
  }
}
